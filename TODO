Global
    - Get rid of X_TABLE constants in favor of __x names.
    - Replace all $_GET, $_POST and $_SESSION accesses with an Input class that
      has input cleaning.
    x Get rid of SOAP garbage.
    - Undo the complete hackness of 1.3 implementation (find in project '1.3')
    - Replace shitty glyphs with the Silk versions.

/admin/index.php
    - Refactor get_x_name into a single method.
    
/admin/mm/mm_listmembers.php
    - Format the current value correctly.

/listmembers.php
    - Format the current value correctly.

/viewmember.php
    - Format the current value correctly.